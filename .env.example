VITE_BACKEND_URL=http://127.0.0.1:8001

# The submitted build does not require provider secrets.
# Add model-provider credentials only in a private backend environment.

# Optional backend hardening for deployed API usage.
CARBONLENS_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
CARBONLENS_USAGE_FILE=/tmp/carbonlens_usage.json
